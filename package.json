{
	"name": "recipes",
	"type": "module",
	"version": "0.0.1",
	"scripts": {
		"dev": "astro dev",
		"build": "astro build",
		"preview": "astro preview",
		"astro": "astro",
		"check-types": "tsc --noEmit",
		"format": "biome format ./src",
		"format:fix": "biome format ./src --write",
		"lint": "pnpm check-types && biome lint && pnpm format"
	},
	"dependencies": {
		"@astrojs/mdx": "^4.3.12",
		"astro": "^5.16.3"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.8"
	}
}
